---
id: 6
slug: subscription-worker-fsm-and-end-to-end-shibuya-integration
title: "Subscription-worker FSM and end-to-end Shibuya integration"
kind: master-plan
created_at: 2026-05-29T20:08:27Z
intention: "intention_01kstnhravebaryq7x3e50z6pz"
---

# Subscription-worker FSM and end-to-end Shibuya integration

This MasterPlan is a living document. The sections Progress, Surprises & Discoveries,
Decision Log, and Outcomes & Retrospective must be kept up to date as work proceeds.


## Vision & Scope

Kiroku is a PostgreSQL-backed event store (repository root `/Users/shinzui/Keikaku/bokuno/kiroku-project/kiroku`, package `kiroku-store`). A **subscription** is a long-lived worker that reads events in order — either the global `$all` stream or one category — and feeds them to a handler one at a time, remembering progress in a durable **checkpoint** row (`kiroku.subscriptions.last_seen`). The subscription runtime lives in `kiroku-store/src/Kiroku/Store/Subscription/Worker.hs`, with a centralized broadcaster in `kiroku-store/src/Kiroku/Store/Subscription/EventPublisher.hs` and a PostgreSQL `LISTEN`/`NOTIFY` wake source in `kiroku-store/src/Kiroku/Store/Notification.hs`.

Kiroku's design is heavily influenced by the Elixir EventStore project (local source at `/Users/shinzui/Keikaku/hub/event-sourcing/eventstore`). EventStore models each subscription as an **explicit finite state machine** (a "finite state machine" is a value that is always in exactly one named state — for EventStore: `initial`, `request_catch_up`, `catching_up`, `subscribed`, `max_capacity`, `disconnected`, `unsubscribed` — with a transition function that names every legal move between states; see `lib/event_store/subscriptions/subscription_fsm.ex`). Kiroku reproduces EventStore's **behavior** (two phases: catch up against history, then go live on notifications; at-least-once delivery; monotonic checkpoints) but tracks its state **implicitly** through which function happens to be executing (`runWorker` → `catchUp` → one of three live loops) plus two mutable cells (`posRef :: IORef GlobalPosition` for the cursor, `statusVar :: TVar SubscriberStatus` for liveness). There is no value you can read to ask "what state is this subscription in," and no single place that enumerates the legal transitions.

That implicit model leaves three concrete behavioral gaps relative to EventStore, each of which this initiative closes:

1. **Overflow is terminal, not backpressure.** When a subscriber's bounded queue fills, the publisher sets `SubscriberStatus` to `Overflowed` and the worker throws `SubscriptionOverflowed` and dies (overflow policy `DropSubscription`), or silently drops events (`DropOldest`). EventStore instead enters a recoverable `max_capacity` state: it stops sending, waits for the consumer to drain, and resumes. Kiroku has no "paused, waiting to resume" state.
2. **No worker-level reconnect.** Reconnect logic exists only at the `LISTEN`/`NOTIFY` listener layer (`Notification.hs`) and as fetch-retry-with-backoff inside `catchUp`. A worker that loses its database pool while live simply dies and propagates the exception; EventStore transitions `subscribed → disconnected → request_catch_up` and resumes from the last acknowledged position.
3. **Per-event dispositions have nowhere to live, and the Shibuya integration is therefore unfinished.** The handler result type `SubscriptionResult` (in `kiroku-store/src/Kiroku/Store/Subscription/Types.hs`) is only `Continue | Stop`. There is no way for a handler to ask that one event be retried later or recorded as a poison event ("dead-lettered") while the subscription advances past it. This most directly hurts `shibuya-kiroku-adapter`, which bridges a Kiroku subscription into the Shibuya queue-processing framework (local source `/Users/shinzui/Keikaku/bokuno/shibuya-project/shibuya`): Shibuya handlers return `AckOk | AckRetry | AckDeadLetter | AckHalt`, but the adapter implements `AckRetry` and `AckDeadLetter` as no-ops because the stream bridge (`kiroku-store/src/Kiroku/Store/Subscription/Stream.hs`) returns `Continue` and lets the checkpoint advance before the Shibuya handler has even run.

A fourth gap is **per-subscription event-type filtering**, again of a different kind: a missing capability rather than a regression. EventStore lets a subscriber pass a `selector` (a function `RecordedEvent -> Bool`) so it only receives events it cares about; filtered-out events are still marked acknowledged so the checkpoint advances past them and progress never stalls (see `lib/event_store/subscriptions/subscription_fsm.ex`, `enqueue_events`/`selected?`). Kiroku has no equivalent: `SubscriptionConfigM` has no selector field, no read statement filters on `event_type`, and the Streamly bridge exposes no filter, so a handler that only cares about one event type must receive and discard every other type itself — and a caller who filters the resulting `Stream` downstream with `Streamly.Data.Stream.filter` does not control checkpointing at all. (There is an `EventFilter` type in `kiroku-store/src/Kiroku/Store/Types.hs`, but it is only for correlation/causation *queries*, not subscriptions.) `RecordedEvent` already carries `eventType :: EventType`, so the data is present; only the subscription-level filter is missing.

A fifth concern is partitioning, but of a different kind: it is not a regression against EventStore, it is an unfinished integration. Kiroku already has **consumer groups** — a named subscription split into N static members, each member receiving the streams whose name hashes to its slot, each with its own checkpoint keyed `(subscription_name, consumer_group_member)`. That work shipped under `docs/masterplans/4-consumer-group-support-for-partitioned-subscriptions.md`. Shibuya independently has its own partitioning vocabulary in `shibuya-core/src/Shibuya/Policy.hs`: an `Ordering` (`StrictInOrder | PartitionedInOrder | Unordered`) and a `Concurrency` mode (`Serial | Ahead Int | Async Int`), validated so that `StrictInOrder` requires `Serial`. The Kiroku adapter only passes a `consumerGroup` field through; it never connects kiroku's member partitioning to Shibuya's `PartitionedInOrder` policy, so an operator who wants a parallel group must hand-build N adapters wired to N processors instead of declaring one partitioned subscription.

After this initiative is complete:

- A subscription worker is always in exactly one named state, readable for observability, with a single documented transition table. A consumer that falls behind is **paused and resumed** rather than killed (configurably), and a worker that loses its database connection while live **reconnects and resumes from its checkpoint** rather than dying.
- A handler — native, Streamly-bridged, or Shibuya — can return a **retry** disposition for one event (the same event is redelivered, bounded, before the checkpoint advances) or a **dead-letter** disposition (the event is recorded in a new `kiroku.dead_letters` table and the checkpoint advances atomically past it). A Shibuya handler returning `AckRetry`/`AckDeadLetter` produces exactly these effects instead of disappearing as a no-op.
- The Shibuya adapter exposes a kiroku consumer group as a **single partitioned subscription** that maps onto Shibuya's `PartitionedInOrder` ordering, so the policy a Shibuya operator declares and kiroku's actual per-member partitioning agree, observably, end to end.
- A subscription can declare an **event-type filter** so its handler — native, Streamly-bridged, or Shibuya — receives only events of the chosen types, while the checkpoint still advances past the filtered-out events so the subscription never stalls. The filter is applied in the worker before delivery, so it composes with the bridge and adapter without changing the stream's element type.
- The subscription's state is **reflected in OpenTelemetry traces**: an opt-in tracer turns the worker's FSM lifecycle into spans (catch-up, live fetches, pause, reconnect, retry, dead-letter), each tagged with the subscription name, target, consumer-group member, checkpoint position, attempt counter, and batch sizes — and those identity attributes ride through the Shibuya adapter onto Shibuya's own per-message spans, so one trace is followable from a Kiroku subscription into Shibuya's processing.

**Explicitly out of scope.** Dynamic consumer-group rebalancing (runtime member reassignment / live group resizing) is not part of this initiative; kiroku's static `(member, size)` model is retained, consistent with the decision recorded in MasterPlan 4. No changes are made to `shibuya-core`; all Shibuya-specific translation stays in `shibuya-kiroku-adapter`, consistent with EP-40's existing decision log. A per-subscription **transform/mapper** (EventStore's `mapper`) is excluded; child plan 4 adds filtering only, because a transform does not affect checkpointing and callers can already `fmap` the typed `Stream IO RecordedEvent` downstream. OpenTelemetry **metric** emission for the new states (a current-state gauge, per-state counters) remains deferred, consistent with the project-wide OTel-metrics deferral (MasterPlan 5). OpenTelemetry **tracing** of subscription state, however, was originally and incorrectly listed here as deferred-and-already-handled — the note claimed "the `kiroku-otel` package already adapts `KirokuEvent` and will pick up new constructors without core changes." That is inaccurate: `kiroku-otel` only contains W3C trace-context propagation (`Kiroku.Otel.TraceContext`); there is no `KirokuEvent`→OTel bridge. Subscription-state **tracing** is therefore brought back into scope as **child plan 5 (EP-5, `docs/plans/44-...`)**; see the Decision Log and Surprises & Discoveries.


## Decomposition Strategy

The initiative splits into five work streams by functional concern, each independently verifiable, sequenced so that no stream reworks another's output. (Child plan 5 was added after the original four shipped — see the Decision Log entry dated 2026-05-30.)

**Child plan 1 — the finite state machine (`docs/plans/41-...`).** This is the foundation. It introduces an explicit `SubscriptionState` value and a transition function in `kiroku-store`, re-expressing today's implicit phases (catch-up, live, stopped) as named states and adding the two missing recoverable states (paused/backpressured, reconnecting). It changes no public handler API and adds no per-event dispositions; its acceptance is that existing subscription behavior is preserved while the new paused/reconnect behaviors become observable and testable. It is first because the other two streams attach new behavior to states, and building them on the current implicit control flow would mean implementing the same logic twice.

**Child plan 2 — per-event retry and dead-letter (`docs/plans/40-...`, the existing EP-40, adopted and rebased).** This extends `SubscriptionResult` with retry and dead-letter constructors, adds a retry policy and the `kiroku.dead_letters` table, makes the Streamly bridge ack-coupled, and redesigns the Shibuya adapter so `AckRetry`/`AckDeadLetter` drive real Kiroku dispositions before the checkpoint advances. EP-40 already exists with substantial design work (its M0 decision log); rather than discard it, this MasterPlan adopts it as child plan 2 and rebases its mechanics so the new dispositions become **transitions and a `Retrying` state in the child-plan-1 FSM** instead of branches in the old imperative `processEvents`. It hard-depends on child plan 1 because retry-with-bounded-attempts is naturally a state (attempt counter as FSM context) and dead-letter-then-advance is a transition out of event processing.

**Child plan 3 — consumer-group ↔ Shibuya policy partitioning (`docs/plans/42-...`).** This connects kiroku's existing static consumer-group partitioning (delivered by MasterPlan 4) to Shibuya's `Ordering`/`Concurrency` policy, so the Shibuya adapter can present a whole group as one `PartitionedInOrder` subscription rather than requiring manual N-adapter fan-out, and so the declared policy and kiroku's real partitioning are reconciled and asserted. It is adapter-and-config work in `shibuya-kiroku-adapter` plus minor `kiroku-store` ergonomics; it must not change `shibuya-core`.

**Child plan 4 — per-subscription event-type filtering (`docs/plans/43-...`).** This adds an event-type filter to `SubscriptionConfigM`, applied **in the worker before delivery** (the decision recorded by the user: an in-memory worker-side filter, not SQL pushdown, so the shared `$all` broadcast publisher in `kiroku-store/src/Kiroku/Store/Subscription/EventPublisher.hs` is untouched), and surfaces it through the Streamly bridge and the Shibuya adapter. Its defining correctness property mirrors EventStore: a filtered-out event must still **advance the checkpoint** (the worker moves its cursor past it) so a highly selective subscription never stalls. Because the filter is applied before the handler/bridge sees the event, it changes no stream element type and composes with the other plans. It carries a soft dependency on child plan 1 because "deliver this event versus skip it but still advance the cursor" is precisely a delivery transition in the FSM, and that is the clean home for the no-stall invariant. Filtering only — no transform/mapper (see Vision & Scope).

**Child plan 5 — OpenTelemetry tracing of subscription state (`docs/plans/44-...`).** This is purely observability and it is additive: it adds no behavior to the worker and changes no core type. It introduces a new module in `kiroku-otel` that consumes the worker's existing `KirokuEvent` stream (the lifecycle events child plans 1–4 already emit) through the `eventHandler` callback and turns FSM state into OpenTelemetry spans, and it populates Shibuya's existing-but-empty `Envelope.attributes` in `shibuya-kiroku-adapter` so the kiroku identity rides through to Shibuya's per-message spans. It depends on no core change because `KirokuEvent` already carries every transition and attribute it needs; `kiroku-store` stays free of any `hs-opentelemetry` dependency. The span model is constrained by a hard fact about the OTel SDK (verified in `hs-opentelemetry`: a span is only exported when it ends), so child plan 5 uses short per-episode and per-batch spans rather than one worker-lifetime span — see the Decision Log. It was added after child plans 1–4 shipped, to close a gap the original scope note wrongly treated as already-handled.

Alternatives considered and rejected: (a) a single mega-ExecPlan — rejected because it would exceed five milestones across `kiroku-store`, `kiroku-store-migrations`, and `shibuya-kiroku-adapter`, the threshold MASTERPLAN.md sets for preferring a MasterPlan. (b) Doing retry/dead-letter (EP-40) first on the current imperative loop and FSM-ifying afterward — rejected because it would implement disposition handling twice and throw the first version away; sequencing the FSM first avoids the rework. (c) Folding partitioning into the adapter changes of child plan 2 — rejected because partitioning has no dependency on dead-letter mechanics and is independently verifiable; merging them would couple two unrelated behaviors and unbalance the plans.


## Exec-Plan Registry

| # | Title | Path | Hard Deps | Soft Deps | Status |
|---|-------|------|-----------|-----------|--------|
| 1 | Explicit subscription-worker finite state machine with recoverable backpressure and live reconnect | docs/plans/41-explicit-subscription-worker-finite-state-machine-with-recoverable-backpressure-and-live-reconnect.md | None | None | Complete |
| 2 | Per-event retry / dead-letter for kiroku subscriptions and the shibuya-kiroku-adapter | docs/plans/40-per-event-retry-and-dead-letter-for-kiroku-subscriptions-and-the-shibuya-adapter.md | EP-1 | EP-3 | Complete |
| 3 | Wire kiroku consumer groups into the Shibuya partitioned-ordering policy model | docs/plans/42-wire-kiroku-consumer-groups-into-the-shibuya-partitioned-ordering-policy-model.md | None | EP-1 | Complete |
| 4 | Per-subscription event-type filtering through the worker, Streamly bridge, and Shibuya adapter | docs/plans/43-per-subscription-event-type-filtering-through-the-worker-streamly-bridge-and-shibuya-adapter.md | None | EP-1 | Complete |
| 5 | OpenTelemetry tracing of subscription-worker state and span attributes end to end through the Shibuya adapter | docs/plans/44-opentelemetry-tracing-of-subscription-worker-state-and-span-attributes-end-to-end-through-the-shibuya-adapter.md | None | EP-1, EP-2, EP-3, EP-4 | Complete |

Status values: Not Started, In Progress, Complete, Cancelled.
Hard Deps and Soft Deps reference other rows by their # prefix (e.g., EP-1, EP-3).


## Dependency Graph

Child plan 1 (FSM) is the root. It has no dependencies and should be implemented first. It defines the `SubscriptionState` type, the transition function, and the per-worker reconnect/pause behavior that the other plans extend.

Child plan 2 (EP-40, retry/dead-letter) **hard-depends** on child plan 1. The hard dependency is real in the MASTERPLAN.md sense — child plan 2's code would not make sense without child plan 1's artifacts. Bounded retry is "redeliver the same event, counting attempts, until success or a maximum, then dead-letter": in the FSM this is a `Retrying` state holding the attempt count as context, with transitions back to event processing or onward to dead-letter. Dead-letter-then-advance is a transition that performs an atomic SQL write and moves the cursor. Implementing these against the current implicit control flow would mean writing disposition handling once now and rewriting it onto the FSM later. Child plan 2 also carries a **soft dependency** on child plan 3 (see Integration Points): both touch the Shibuya adapter's bridge, and the dead-letter table is keyed by consumer-group member, so the two plans must agree on the per-member shape even though neither blocks the other's start.

Child plan 3 (partitioning) has **no hard dependency** and is largely independent: its core work — mapping a kiroku consumer group onto Shibuya's `Ordering`/`Concurrency` policy in the adapter via a helper that emits one policy-pinned Shibuya processor per member — does not require the FSM and does not change the Streamly bridge (see Integration Points). It carries a weak **soft dependency** on child plan 1 only for landing order: the cleanest place to surface per-member state for the policy-reconciliation tests is the FSM's observable state, and finalizing child plan 3 after child plan 1 lets those tests assert against named states rather than implicit phases.

Child plan 4 (event-type filtering) has **no hard dependency** and carries a **soft dependency** on child plan 1: the filter is applied in the FSM's delivery transition, and the "skip but still advance the cursor" invariant lives most naturally there, so finalizing it after child plan 1 avoids implementing the skip logic against the implicit control flow and then moving it onto the FSM. It is otherwise independent of child plans 2 and 3 and can proceed in parallel with them; the only coupling is that it edits the shared `SubscriptionConfigM` record and the shared adapter config, which is an additive integration point (see below).

Child plan 5 (OTel tracing) has **no hard dependency** and carries a **soft dependency** on child plans 1–4: it consumes the `KirokuEvent` lifecycle stream that they emit (child plan 1's started/caught-up/paused/resumed/reconnecting/stopped events; child plan 2's retrying/dead-lettered events; child plan 3's per-member `SubscriptionGroupContext`; child plan 4's filtered-but-checkpoint-advancing delivery, which simply produces fewer fetch spans). The soft dependency is real only in that the events it traces must exist first — which they now do, all four plans being Complete — and that the adapter config it extends (`KirokuAdapterConfig` / `KirokuConsumerGroupConfig`) was shaped by child plans 2–4. It needs no core change and adds no `hs-opentelemetry` dependency to `kiroku-store`: the bridge is the existing `eventHandler :: Maybe (KirokuEvent -> IO ())` callback, and the Shibuya side fills the already-present `Envelope.attributes` field. Because child plans 1–4 are Complete, child plan 5 can proceed immediately and in isolation.

Recommended order is 1 → (2, 3, and 4 in parallel) → 5, with a single reconciliation checkpoint on the shared `SubscriptionConfigM` and Streamly bridge before any of 2, 3, or 4 finalizes: confirm that child plan 4's filter is applied *before* child plan 2's ack-coupled bridge handler (so retry/dead-letter only ever apply to delivered events), that child plan 3 does not introduce a bridge item type competing with child plan 2's ack-coupled item, and that all config-field additions land on the one `SubscriptionConfigM` record without conflicting defaults.


## Integration Points

**`SubscriptionResult` and the FSM transition function (`kiroku-store/src/Kiroku/Store/Subscription/Types.hs`, `.../Worker.hs`).** Child plan 1 defines `SubscriptionState` and the transition function and keeps `SubscriptionResult` as `Continue | Stop`. Child plan 2 extends `SubscriptionResult` with retry and dead-letter constructors and adds the matching FSM states/transitions. Child plan 1 is responsible for defining the transition function in a way that is exhaustively pattern-matched, so that child plan 2 adding a constructor produces a compile error at every site that must handle it (rather than a silent fallthrough). Child plan 1 must leave a documented extension seam (a single `step :: SubscriptionState -> Input -> (SubscriptionState, Effect)`-style function) that child plan 2 extends; child plan 2 must not fork a parallel state type.

**The `SubscriptionConfigM` record (`kiroku-store/src/Kiroku/Store/Subscription/Types.hs`).** Child plan 2 adds a retry-policy field; child plan 4 adds an event-type-filter field. Both are additive and both must update `defaultSubscriptionConfig` so existing callers (which build configs via the smart constructor) inherit a no-op default — `Continue`-style retry disabled and "no filter / all event types" respectively. There is no ordering constraint between the two additions, but the reconciliation checkpoint must confirm the merged record compiles with both fields and that neither plan removes or renames the fields the other relies on. Each plan is responsible for its own field and its own default; whichever lands second rebases onto the merged record rather than reverting the first.

**Filter-before-delivery ordering (child plan 4 relative to child plan 2).** Child plan 4 applies the event-type filter in the worker *before* the event reaches the handler. Child plan 2 makes the handler path (via the ack-coupled Streamly bridge) able to return retry/dead-letter dispositions. These must compose in one order only: **filter first, then deliver to the (possibly ack-coupled) handler.** A filtered-out event must never reach the bridge, never receive a reply, and never be retried or dead-lettered — the worker simply advances its cursor past it. Child plan 4 is responsible for placing the filter at the point in the FSM delivery transition that precedes the handler effect; child plan 2 must not move disposition handling ahead of the filter. The reconciliation checkpoint verifies this ordering with a test: a filtered-out event type is never delivered even when the handler would dead-letter it.

**The Streamly bridge `Stream.hs` (`kiroku-store/src/Kiroku/Store/Subscription/Stream.hs`).** Child plan 2 makes it **ack-coupled** (each emitted item carries a one-shot reply variable; the bridge handler blocks until the consumer replies with a `SubscriptionResult`, and only then does the worker checkpoint/retry/dead-letter). Child plan 2 is the sole owner of the bridge item type and defines it as approximately `{ event, attempt, reply }` (names finalized in EP-40's M0). Child plan 3 originally looked like it would need to add **per-member identity** to this item, but its research established that Shibuya's runner does **not** route by an envelope partition key — `Envelope.partition` is only an OpenTelemetry attribute and `Concurrency (Async n)` is unkeyed fan-out (see `shibuya-core/src/Shibuya/Runner/Supervised.hs`). Child plan 3 therefore runs **one Shibuya processor per kiroku consumer-group member**, so member identity rides the `ProcessorId` (`"<name>-member-<m>"`), not the stream item. The reconciliation outcome is: child plan 3 does **not** extend the bridge item; it consumes child plan 2's per-event stream unchanged. The reconciliation checkpoint named in the Dependency Graph is satisfied by confirming, before either plan finalizes, that child plan 3 does not introduce a competing bridge item type and that child plan 2's ack-coupled item is the only one.

**The `kiroku.dead_letters` table (`kiroku-store-migrations/sql-migrations/`).** Child plan 2 defines this table with a `consumer_group_member` column (default 0) so that a dead-letter row is attributable to the member that produced it. Child plan 3 does not modify the schema but relies on dead-letters being per-member when it presents a group through the adapter. Child plan 2 is responsible; child plan 3 consumes.

**The Shibuya adapter config `KirokuAdapterConfig` (`shibuya-kiroku-adapter/src/Shibuya/Adapter/Kiroku.hs`).** Child plan 2 changes how the adapter's `AckHandle.finalize` maps `AckDecision` to a Kiroku disposition. Child plan 3 changes how the adapter presents a consumer group (the `consumerGroup` field and any new policy field). Child plan 4 adds an event-type-filter field to `KirokuAdapterConfig` (and to the consumer-group config that child plan 3 introduces) that it forwards into the underlying `SubscriptionConfigM`; because filtering happens worker-side before the bridge, child plan 4 needs no change to `Convert.hs` or the ack handle. All three edit `kirokuAdapter`. Child plan 3 owns the config-shape/policy changes, child plan 2 owns the ack-handle changes, child plan 4 owns the filter field. They edit adjacent code in the same function, so the reconciliation checkpoint must confirm all sets of edits compose (the ack handle, the member-aware presentation, and the forwarded filter coexist on the same adapter and `Ingested` value).

**The `KirokuEvent` constructor set (`kiroku-store/src/Kiroku/Store/Observability.hs`) and the `eventHandler` callback.** Child plans 1–4 each *added* `KirokuEvent` constructors (started/caught-up/paused/resumed/reconnecting/fetched/stopped/retrying/dead-lettered) and the `SubscriptionGroupContext`. The constructor set is documented as *additive* (new constructors are added, never changed; consumers that pattern-match exhaustively get a `-Wincomplete-patterns` warning, never a silent miss). Child plan 5 is the first *consumer* of this surface for tracing: it reads these constructors through the existing `eventHandler :: Maybe (KirokuEvent -> IO ())` callback and turns them into spans, in a new module in `kiroku-otel`. Child plan 5 owns the new `kiroku-otel` module and must not change the `KirokuEvent` type, `kiroku-store`, or the FSM — it is a pure read-side consumer. If any future child plan adds a subscription `KirokuEvent` constructor, child plan 5's exhaustive handler will flag it at compile time, which is the intended seam.

**Shibuya's `Envelope.attributes` (`shibuya-core/src/Shibuya/Core/Types.hs`, consumed in `shibuya-core/src/Shibuya/Runner/Supervised.hs`).** The Shibuya runner already merges `envelope.attributes :: HashMap Text Attribute` into its per-message span (left-biased union, adapter keys win). Child plans 1–4 left this field empty (`attributes = HashMap.empty` in `shibuya-kiroku-adapter/src/Shibuya/Adapter/Kiroku/Convert.hs`). Child plan 5 populates it with `kiroku.subscription.name`, `kiroku.consumer_group.member`, `kiroku.event.type`, and `kiroku.event.global_position`, threading the subscription name and member index from the adapter configs (`KirokuAdapterConfig` / `KirokuConsumerGroupConfig`, shaped by child plans 2–4) into the conversion. This composes with the W3C trace-context propagation already done in `toEnvelope` (which sets the span *parent*); the new attributes are *tags* on the child span. Child plan 5 owns this edit; it requires **no `shibuya-core` change** and no change to `Convert.hs`'s ack-handle logic.


## Progress

Track milestone-level progress across all child plans. Each entry names the child plan
and the milestone. This section provides an at-a-glance view of the entire initiative.

- [x] EP-1 (FSM): M1 — define `SubscriptionState` and an exhaustive transition function; re-express today's catch-up/live/stopped phases as named states with no behavior change. (Done 2026-05-29: `Fsm.hs` + driver refactor of `Worker.runWorker`; `164 examples, 0 failures`; all downstream packages link.)
- [x] EP-1 (FSM): M2 — recoverable backpressure: replace terminal `Overflowed` with a `Paused` state that resumes when the consumer drains (configurable). (Done 2026-05-29: `PauseAndResume` policy + default, publisher `Paused` status, `Paused`→re-catch-up recovery, `Paused`/`Resumed` events; `166 examples, 0 failures`.)
- [x] EP-1 (FSM): M3 — worker-level `Reconnecting` state: a live worker that loses its pool re-enters catch-up from its checkpoint instead of dying. (Done 2026-05-29: Category/consumer-group live loops bubble fetch errors → `Reconnecting` → re-catch-up; `KirokuEventSubscriptionReconnecting`; Category-subscription reconnect test; `167 examples, 0 failures`. AllStreams live is publisher-fed and has no worker fetch — see EP-41 Decision Log.)
- [x] EP-1 (FSM): M4 — observability + tests: expose current state; regression tests for no-missed-events, monotonic checkpoints, pause/resume, reconnect. (Done 2026-05-29: `currentState` handle accessor + state `TVar`; Paused/Resumed/Reconnecting events; arch doc + CHANGELOG updates; `Test/SubscriptionState.hs`; `169 examples, 0 failures`.)
- [x] EP-2 (retry/DL): M0 — finalized the Kiroku-native disposition API, retry policy, dead-letter reason, and attempt reporting (rebased onto the FSM). (Done 2026-05-29: see EP-40 Decision Log; `Retry RetryDelay` / `DeadLetter DeadLetterReason`, `RetryPolicy`/`defaultRetryPolicy`, in-memory attempts.)
- [x] EP-2 (retry/DL): M1 — added retry/dead-letter dispositions in `kiroku-store` with a surfaced `Retrying` FSM state; disposition mechanics in the single `processEvents` delivery primitive (deviation from the new-FSM-Input design — see Surprises). (Done 2026-05-29: `kiroku-store` 172 examples, 0 failures.)
- [x] EP-2 (retry/DL): M2 — `kiroku.dead_letters` forward migration + atomic insert-and-checkpoint / read SQL statements; `docs/user/schema.md` updated. (Done 2026-05-29.)
- [x] EP-2 (retry/DL): M3 — ack-coupled Streamly bridge (`subscriptionAckStream`/`AckItem`) + Shibuya adapter `AckOk`/`AckRetry`/`AckDeadLetter`/`AckHalt` mapping via `AckHandle.finalize`. (Done 2026-05-29.)
- [x] EP-2 (retry/DL): M4 — tests, docs, changelogs across the three packages; test-harness fix to apply all forward migrations. (Done 2026-05-29: kiroku-store 172, adapter 10, migrations 1 — all 0 failures.)
- [x] EP-3 (partitioning): M1 — map a kiroku `ConsumerGroup` onto Shibuya `Ordering`/`Concurrency`; reject invalid combinations early. (Done 2026-05-29: `consumerGroupPolicy` reuses `validatePolicy StrictInOrder`; `Serial` → `(PartitionedInOrder, Serial)`, `Ahead`/`Async` → `InvalidPolicyCombo`. `KirokuConsumerGroupConfig`/`defaultConsumerGroupConfig` added.)
- [x] EP-3 (partitioning): M2 — adapter presents a whole group as one `PartitionedInOrder` source, no manual N-adapter wiring. (Done 2026-05-29: `kirokuConsumerGroupProcessors` yields `N` policy-pinned `QueueProcessor`s with `ProcessorId "<name>-member-<m>"`; one Shibuya processor **per member** — not a member-aware bridge — per the routing finding. No `shibuya-core` changes; bridge unchanged.)
- [x] EP-3 (partitioning): M3 — end-to-end test: a size-N group through the adapter delivers a disjoint, complete partition whose union is `[1..total]`. (Done 2026-05-29: `15 examples, 0 failures`; size-4 group, one call, 20 streams × 2 events → `sort positions == [1..40]` with no duplicates, all 20 streams covered, `Async` rejected before any subscription opens. Per-member sets asserted at the union level — see EP-42 Decision Log.)
- [x] EP-4 (filtering): M1 — added `EventTypeFilter (AllEventTypes | OnlyEventTypes !(Set EventType))` + `eventTypeMatches` and an `eventTypeFilter` field on `SubscriptionConfigM` (default `AllEventTypes`); applied the predicate in `processEvents`'s per-event branch (before the handler) so filtered-out events skip delivery while the batch-tail checkpoint advances past them. (Done 2026-05-29: deviation from the "FSM delivery transition" wording follows EP-2 — `processEvents` is the single delivery primitive; see Surprises.)
- [x] EP-4 (filtering): M2 — filter forwarded through the Streamly bridge (no element-type change; Haddock note) and through the Shibuya adapter (`KirokuAdapterConfig` + EP-42's `KirokuConsumerGroupConfig`, threaded into each per-member adapter; `EventTypeFilter (..)` re-exported). Builds clean; adapter suite green. (Done 2026-05-29; dedicated adapter delivery-path test deferred — see Surprises.)
- [x] EP-4 (filtering): M3 — `kiroku-store-test` proves selective delivery, checkpoint-past-trailing-filtered, **no-stall** (1 A / 1000 B / 1 A → `last_seen = 1002`, 2 deliveries), and **filter-before-dead-letter** (a dead-letter-everything handler is never invoked behind a no-match filter → 0 rows, checkpoint advances). (Done 2026-05-29: `kiroku-store-test` 174 examples, 0 failures; adapter 15, 0. Consumer-group+filter adapter delivery assertion deferred — see Surprises.)
- [x] EP-5 (OTel tracing): M1 — new `Kiroku.Otel.Subscription` module in `kiroku-otel` turning the `KirokuEvent` lifecycle stream into spans (per-episode `catchup`/`paused`/`reconnecting`/`retrying` spans + per-batch `fetch` spans; `kiroku.*` attribute set), with the export-on-end-constrained model (no worker-lifetime span); unit tests drive synthetic `KirokuEvent` sequences through an in-memory exporter and assert span names/attributes and that every episode span ends. Test-only deps `hs-opentelemetry-sdk` + `hs-opentelemetry-exporter-in-memory`. (Done 2026-05-30: `subscriptionTraceHandler`, `MVar`-keyed by `(name, member)`; `kiroku-otel-test` 13 examples, 0 failures. SDK + in-memory exporter pinned from the same git tag as the api 0.3.0.0 — see Surprises.)
- [x] EP-5 (OTel tracing): M2 — populate Shibuya `Envelope.attributes` in `shibuya-kiroku-adapter` (`kiroku.subscription.name`, `kiroku.consumer_group.member`, `kiroku.event.type`, `kiroku.event.global_position`), threaded from `KirokuAdapterConfig`/`KirokuConsumerGroupConfig`; adapter test asserts the attributes appear on the produced `Envelope` for single and consumer-group paths. No `shibuya-core` change. (Done 2026-05-30: `KirokuEnvelopeAttrs` in `Convert.hs`, threaded via `kirokuAdapter`; `shibuya-kiroku-adapter-test` 20 examples, 0 failures.)
- [x] EP-5 (OTel tracing): M3 — docs (module Haddock incl. the export-on-end limitation and batch-processor requirement; arch/user OTel-tracing subsection), CHANGELOGs (`kiroku-otel`, `shibuya-kiroku-adapter`), an end-to-end example/test, and correction of this MasterPlan's prior inaccurate "`kiroku-otel` already adapts `KirokuEvent`" claim. (Done 2026-05-30: `docs/user/opentelemetry.md` "Tracing Subscription State" section + cross-links from `observability.md`/`shibuya-adapter.md`; both CHANGELOGs; end-to-end demonstrated via M1 synthetic + M2 adapter tests + documented wiring rather than a DB-backed `kiroku-otel` test — see EP-44 Decision Log; the false claim was already corrected in Vision & Scope when EP-5 was added. `cabal build all` clean.)


## Surprises & Discoveries

Document cross-plan insights, dependency changes, scope adjustments, or unexpected
interactions between child plans. Provide concise evidence.

- Consumer groups already exist. The partitioning work (child plan 3) was initially scoped as "kiroku uses consumer groups, the Shibuya adapter is not plugged into that yet." Research confirmed kiroku's consumer-group *runtime* shipped under MasterPlan 4 (`docs/masterplans/4-consumer-group-support-for-partitioned-subscriptions.md`); the adapter already passes a `consumerGroup` field through (`shibuya-kiroku-adapter/src/Shibuya/Adapter/Kiroku.hs`). The remaining gap is purely the **policy reconciliation** between kiroku's static member partitioning and Shibuya's `Ordering`/`Concurrency` model — not building consumer groups. Child plan 3 is scoped accordingly and references MasterPlan 4 as completed prior art rather than re-deriving it.

- EP-1 (FSM) shipped (2026-05-29). The artifacts the other child plans depend on are now in place, with three findings that affect them:
  - **The extension seam EP-2 must use** is `step :: SubscriptionState -> Input -> (SubscriptionState, [Effect])` in `kiroku-store/src/Kiroku/Store/Subscription/Fsm.hs`, compiled `-Werror=incomplete-patterns`. EP-2 adds its retry/dead-letter `SubscriptionResult` constructors and matching `Input` constructors (and a `Retrying` state) **in place** here; the per-event handler result is surfaced to `step` via the driver's `DeliverBatch` interpretation in `Worker.runWorker` (today `processEvents` collapses `Continue`/`Stop` and the driver feeds `HandlerStopped` — EP-2 restructures that delivery point to emit its new inputs). Do not fork a parallel state type.
  - **`SubscriptionStopReason` moved** from `Kiroku.Store.Observability` into `Kiroku.Store.Subscription.Fsm` (re-exported from `Observability`, so the `KirokuEvent` API is unchanged) to keep the module graph acyclic now that `Subscription.Types.SubscriptionHandleM` carries a `currentState :: m SubscriptionState` field. EP-2/EP-4, which also edit `Subscription.Types`, inherit this layout.
  - **The default `OverflowPolicy` is now `PauseAndResume`** (was `DropSubscription`), and `EventPublisher.SubscriberStatus` gained a `Paused` value. EP-4's filter field and EP-2's retry-policy field both land on the same `SubscriptionConfigM`/`defaultSubscriptionConfig`; whichever rebases must preserve the `PauseAndResume` default. The Shibuya adapter already overrides `overflowPolicy = DropSubscription` explicitly, so EP-3 is unaffected by the default change.
  - **Reconnect (`Reconnecting`) applies only to fetching subscriptions** (Category / consumer-group): AllStreams live delivery is publisher-fed and performs no worker fetch. EP-3, which presents a consumer group as one partitioned subscription, gets observable per-member `Reconnecting`/`CatchingUp`/`Live` states for its policy-reconciliation tests (its soft dependency on EP-1).

- EP-2 (retry/dead-letter) shipped (2026-05-29). It deviates from one Integration Point and surfaces two cross-plan findings:
  - **Disposition mechanics live in the driver's delivery primitive, not in new FSM `Input` constructors.** The Integration Point envisioned EP-2 adding retry/dead-letter `Input` constructors that drive `step`. Instead, EP-2 added the `Retry`/`DeadLetter` `SubscriptionResult` constructors and a surfaced `Retrying` `SubscriptionState`, and implemented the bounded-retry + atomic-dead-letter mechanics inside the single delivery primitive `processEvents` (shared by the FSM `DeliverBatch` effect and the Category / consumer-group live loops). Rationale: a fully `step`-driven design would have to thread whole-batch vectors through the FSM alphabet and would split disposition handling across the FSM path (AllStreams) and the imperative live loops (Category / consumer-group), duplicating it; one primitive keeps behavior identical on every path. The compile-time safety the MasterPlan valued is preserved differently but fully: adding the `SubscriptionResult` constructors forces a compile error at every `case`-on-`SubscriptionResult` site, and adding the `Retrying` state forces one in `step`/`stateCursor` (both `-Werror=incomplete-patterns`). **Impact on EP-4:** the filter-before-delivery ordering still holds trivially — the filter must be applied inside `processEvents` *before* the `handler config event` call, which is the one place delivery happens; a filtered-out event never reaches the disposition logic. EP-4 should place its filter at the top of `processEvents`'s per-event loop (before `handler`), advancing the cursor without delivering.
  - **The shared test harness only applied the bootstrap migration.** `kiroku-test-support`'s `Kiroku.Test.Postgres.migrateTestDatabase` was bootstrap-only, so `kiroku-store` / adapter test databases did not get forward migrations. EP-2 changed it to apply every `sql-migrations/*.sql` in filename order. **Impact on EP-4:** if EP-4 adds a migration (it should not need to — its filter is in-memory, worker-side), the harness now picks it up automatically; either way EP-4 inherits a harness that reflects the full schema.

- EP-3 (partitioning) shipped (2026-05-29). Adapter-only, exactly as scoped; one finding for EP-4:
  - **A new whole-group config record exists that EP-4 must also extend.** EP-3 added
    `KirokuConsumerGroupConfig` (and `defaultConsumerGroupConfig`) in
    `shibuya-kiroku-adapter/src/Shibuya/Adapter/Kiroku.hs`, distinct from `KirokuAdapterConfig`. Per
    this MasterPlan's Integration Points, EP-4 adds an event-type-filter field to *both* the adapter
    config and "the consumer-group config that child plan 3 introduces" — that record is now
    `KirokuConsumerGroupConfig`. EP-4 must add its filter field there too and have
    `kirokuConsumerGroupProcessors` forward it into each per-member `KirokuAdapterConfig` (the helper
    builds one per member), so a filtered partitioned group behaves like a filtered single
    subscription. The fields are duplicated across the two records under `DuplicateRecordFields`;
    EP-4's additions are purely additive.
  - **The helper takes a single shared `Handler es RecordedEvent`** and a Shibuya handler never sees
    its `ProcessorId`/member; this is why EP-3's e2e test asserts the disjoint+complete partition at
    the union level (no duplicate global position ⇔ disjoint; `== [1..40]` ⇔ complete) rather than
    per member. Not a constraint on EP-4 (its filter is worker-side, upstream of the handler), but
    noted for anyone extending the e2e tests.

- EP-4 (event-type filtering) shipped (2026-05-29). Implemented exactly on the seams the earlier plans left:
  - **Filter lives in `processEvents`, before the handler — the EP-2 ordering held trivially.** EP-2's Decision Log predicted EP-4 should "place its filter at the top of `processEvents`'s per-event loop (before `handler`)"; that is exactly where it went. A filtered-out event skips the `handler config event` call and falls through to `go (i+1)`, so it can never reach the ack-coupled bridge and can never be retried or dead-lettered, while the unchanged batch-tail `saveCheckpoint` advances the cursor past it. The `kiroku-store` filter-before-dead-letter test pins this directly (no adapter needed): a handler that dead-letters everything writes **zero** dead-letter rows behind a no-match filter. So the MasterPlan's "filter-before-delivery" reconciliation checkpoint is satisfied in the worker itself.
  - **The shared `SubscriptionConfigM` merged cleanly.** EP-4's `eventTypeFilter` field and EP-2's `retryPolicy` field coexist on the one record; `defaultSubscriptionConfig` keeps EP-1's `PauseAndResume` default and now also defaults `eventTypeFilter = AllEventTypes`. Every full record literal in the test/bench suites was updated (umbrella re-export of `Subscription.Types` makes `AllEventTypes` available everywhere for free).
  - **EP-3's `KirokuConsumerGroupConfig` extended, as it flagged.** EP-3's Surprises required EP-4 to add the filter field to *both* the adapter config and the group config and forward it into each per-member `KirokuAdapterConfig`; done. `EventTypeFilter (..)` is re-exported from the adapter.
  - **Adapter delivery-path tests landed.** `shibuya-kiroku-adapter-test` gained a single-adapter filter test (filtered `kirokuAdapter` over a mixed A/B stream delivers only the As — positions `[1,3,5]`) and a consumer-group+filter test (size-4 group via `kirokuConsumerGroupProcessors` with the same filter on every member, 20 `[A,B]` streams → delivered A positions `sort == [1,3..39]`, disjoint + complete). Suite now 17 examples, 0 failures. (These were briefly blocked mid-session by an editor file-read display outage; once readable they were authored against the existing Shibuya `runApp`/drain harness and pass.)

- Shibuya does not route by partition key, which forces the adapter shape and removes a presumed bridge dependency. Child plan 3's research read `shibuya-core/src/Shibuya/Runner/Supervised.hs` and found that the runner collapses `Concurrency` to a single integer and runs `parMapM` over one inbox: `Async n` is **unkeyed** fan-out with no per-partition affinity, and `Envelope.partition` is used only as an OpenTelemetry attribute. Consequently a single Shibuya processor cannot honor kiroku's per-stream ordering, and the adapter must run **one Shibuya processor per kiroku member** (each member's own processor is `StrictInOrder` + `Serial`; the group is labelled `PartitionedInOrder`). This is why child plan 3 needs no change to the Streamly bridge and why child plan 2 solely owns the ack-coupled bridge item (`{ event, attempt, reply }`). Evidence: `validatePolicy StrictInOrder (Async _)` returns `Left (InvalidPolicyCombo ...)` in `shibuya-core/src/Shibuya/Policy.hs`, and `processUntilDrained`'s `maxConc` handling in `Runner/Supervised.hs`.

- The original OTel scope note was wrong, and the OTel SDK forces the span model (2026-05-30, while adding EP-5). Two findings shaped child plan 5:
  - **`kiroku-otel` does not adapt `KirokuEvent`.** Vision & Scope originally deferred OTel work with the claim that "the `kiroku-otel` package already adapts `KirokuEvent` and will pick up new constructors without core changes." Inspection showed `kiroku-otel` contains only `Kiroku.Otel.TraceContext` — W3C `traceparent`/`tracestate` propagation into and out of an event's `metadata` JSONB column. There is **no** `KirokuEvent`→OTel bridge. So subscription-state *tracing* was never "already handled"; it was simply missing. EP-5 builds the bridge as a `KirokuEvent -> IO ()` handler in a new `kiroku-otel` module, consumed via the existing `eventHandler` callback, with no `kiroku-store` change. The inaccurate sentence in Vision & Scope was corrected when EP-5 was added.
  - **A span exports only when it ends — verified in `hs-opentelemetry`.** `endSpan` (in `hs-opentelemetry-api`, `OpenTelemetry/Trace/Core.hs` ~line 990) sets the end timestamp and calls `tracerProviderOnEnd ... imm` (line 1018), which fans out to `spanProcessorOnEnd` (lines 1225–1226); the batch/OTLP exporters export from `onEnd`, and there is no partial/snapshot export of an in-flight span (`SpanProcessor` record at `OpenTelemetry/Internal/Trace/Types.hs:117`). Therefore a single span held open for a worker's whole lifetime would be invisible until the worker stopped and lost on a crash — so EP-5 must use short per-episode spans (opened on a state-entry event, ended on the matching state-exit event) plus short per-batch spans during `Live`, not one lifetime span. The accepted limitation: an *in-progress* (unresolved) episode does not appear in the backend until it ends; real-time "current state right now" is served instead by the existing `currentState` handle accessor and the `KirokuEvent` log stream, and ultimately by a deferred state-gauge metric. Source read via `mori` at `/Users/shinzui/Keikaku/hub/haskell/hs-opentelemetry-project`.


- EP-5 (OTel tracing) shipped (2026-05-30). Additive and read-side exactly as scoped — no `kiroku-store` change, no `hs-opentelemetry` dependency added to the core, no `shibuya-core` change. Three findings worth recording:
  - **The project resolves `hs-opentelemetry-api` 0.3.0.0, not 0.4.** `cabal.project` pins the api from GitHub at tag `adc464b…` (version 0.3.0.0, confirmed in `dist-newstyle/cache/plan.json`), while the `mori`-registered checkout is on the 0.4 branch. The 0.4 `newEvent`/`newEventWith` helpers (`@since 0.4.1.0`) do not exist in 0.3.0.0, so `Kiroku.Otel.Subscription` builds `NewEvent {..}` directly. Read the *resolved* source under `dist-newstyle/src`, not the `mori` checkout.
  - **`OpenTelemetry.Attributes.addAttributes` is a left-biased union**, so re-setting an attribute (e.g. refreshing a checkpoint or attempt counter on an open span) is silently dropped; the singular `addAttribute` (an `insert`) is required for updates. The tracer routes all attribute writes through a `setAttrs` helper built on `addAttribute`.
  - **The SDK + in-memory exporter had to be pinned from the same git tag as the api.** Because the api is a git `source-repository-package`, the test-only `hs-opentelemetry-sdk` and `hs-opentelemetry-exporter-in-memory` must come from the same tag (`adc464b…`, subdirs `sdk` and `exporters/in-memory`) so they link against the matching 0.3.0.0 internals; the solver picked `hs-opentelemetry-sdk-0.1.0.1`. These are test-only; the `kiroku-otel` library still depends only on `hs-opentelemetry-api`.
  - **End-to-end was demonstrated without a DB-backed `kiroku-otel` test.** M1 drives the handler with the exact `KirokuEvent` constructors the real worker emits through an in-memory exporter (which only receives *ended* spans, so each assertion doubles as proof the episode closed), and M2 proves the adapter attributes on a real `RecordedEvent`; the wiring is documented in `docs/user/opentelemetry.md`. A DB harness in the deliberately-light `kiroku-otel` package was judged disproportionate — see EP-44's Decision Log.


## Decision Log

- Decision: Decompose into three child plans (FSM foundation; retry/dead-letter; partitioning), with the FSM first.
  Rationale: Functional-concern split with independent verifiability; sequencing the FSM first prevents implementing disposition handling twice. See Decomposition Strategy.
  Date: 2026-05-29.

- Decision: The subscription-worker FSM is a faithful EventStore-style machine that adds the two missing recoverable states (paused backpressure, live reconnect), not merely a formalization of current behavior.
  Rationale: User direction. These two states close real behavioral gaps (terminal overflow; worker dies on live DB loss) rather than only renaming today's phases.
  Date: 2026-05-29.

- Decision: Adopt existing EP-40 (`docs/plans/40-...`) as child plan 2 in place, rather than superseding it with a fresh plan.
  Rationale: User direction. EP-40 carries substantial validated design work (M0 decision log, ack-coupled-bridge analysis). It is updated to add the MasterPlan parent and to rebase its mechanics onto child plan 1's FSM; nothing is discarded.
  Date: 2026-05-29.

- Decision: Partitioning (child plan 3) is adapter-only with kiroku's static `(member, size)` membership; no dynamic rebalancing and no `shibuya-core` changes.
  Rationale: User direction, consistent with MasterPlan 4's static-membership decision and EP-40's "do not change shibuya-core" decision.
  Date: 2026-05-29.

- Decision: Child plan 2 hard-depends on child plan 1; child plan 3 soft-depends on child plan 1 and integration-depends on child plan 2 via the shared Streamly bridge.
  Rationale: Retry-with-attempts and dead-letter-then-advance are naturally FSM states/transitions; the ack-coupled bridge (EP-40) and the member-aware bridge (EP-3) edit the same item type and must agree. See Dependency Graph and Integration Points.
  Date: 2026-05-29.

- Decision: Add child plan 4 — per-subscription event-type filtering — as an in-memory, worker-side filter applied before delivery, filtering only (no transform/mapper).
  Rationale: User identified type filtering as another missing capability; research confirmed kiroku has none today (no selector field, no SQL type predicate, no bridge filter), while EventStore supports it via an in-memory `selector` that still advances the checkpoint past filtered events. The user chose an in-memory worker-side filter over SQL pushdown to keep the shared `$all` broadcast publisher untouched, and filtering-only over including a mapper because a transform does not affect checkpointing and callers can `fmap` the typed stream downstream.
  Date: 2026-05-29.

- Decision: Child plan 4 soft-depends on child plan 1 and must apply its filter before child plan 2's handler/bridge (filter-then-deliver), and is otherwise independent of child plans 2 and 3.
  Rationale: "Skip but still advance the cursor" is an FSM delivery-transition invariant (cleanest on child plan 1); a filtered-out event must never reach the ack-coupled bridge, so it can never be retried or dead-lettered. See Dependency Graph and Integration Points.
  Date: 2026-05-29.

- Decision: EP-2 implements retry/dead-letter mechanics in the worker's single delivery primitive (`processEvents`) with a surfaced `Retrying` observability state, rather than threading new FSM `Input` constructors through `step` as the Integration Point envisioned.
  Rationale: A fully `step`-driven design would carry whole-batch vectors in the FSM alphabet and would duplicate disposition handling across the FSM `DeliverBatch` path (AllStreams) and the imperative Category / consumer-group live loops; one delivery primitive keeps behavior identical on all paths. Compile-time exhaustiveness is preserved via the `SubscriptionResult` and `SubscriptionState` match sites (both `-Werror=incomplete-patterns`). The filter-before-delivery contract for EP-4 still holds: EP-4 filters at the top of `processEvents`'s per-event loop, before the handler call. See Surprises & Discoveries.
  Date: 2026-05-29.

- Decision: `AckHalt` keeps the adapter's prior cancel semantics (no checkpoint advance; the halting event replays on restart) rather than mapping to Kiroku `Stop`.
  Rationale: Halting means "stop without acknowledging this event"; cancellation models that and preserves the existing adapter contract. Recorded per EP-40 M3's requirement to log the `AckHalt` choice.
  Date: 2026-05-29.

- Decision: Keep EP-4's `EventTypeFilter` as a closed sum (do **not** replace it with a bare `RecordedEvent -> Bool`), and add a *separate, optional* `selector :: Maybe (RecordedEvent -> Bool)` field on `SubscriptionConfigM` for arbitrary predicate filtering.
  Rationale: The user asked, before shipping the API, whether `EventTypeFilter` should instead be a boolean-returning function (EventStore's `selector` shape) for flexibility. Evaluation: a bare function gains expressiveness (predicates over the whole `RecordedEvent`, e.g. metadata) but permanently forfeits three properties this system relies on — (1) **SQL pushdown** on fetching (`Category` / consumer-group) subscriptions, where a `Set EventType` can be lowered into the catch-up read but a closure cannot; (2) `Eq`/`Show` for the existing test suite and diagnostics; (3) introspectability for any future metrics/admin surface. So the steady-state type filter stays the introspectable closed sum, and the opaque predicate is added *alongside* it as `selector` (default `Nothing`), reserved for ad-hoc/operational filtering — most often a one-off catch-up reprocess that narrows by metadata while investigating a problem. The two compose as a logical AND (`shouldDeliver`); a `selector`-rejected event obeys the same no-stall / checkpoint-advances invariant as a type-filtered one, and (being applied in `processEvents` before the handler) is never retried or dead-lettered.
  Date: 2026-05-30.

- Decision: Forward `selector` through the Shibuya adapter (`KirokuAdapterConfig` and `KirokuConsumerGroupConfig`), alongside the already-forwarded `eventTypeFilter`.
  Rationale: User direction — the adapter is the path most users take, so the escape hatch should be reachable there too, not only on native subscriptions. The initial follow-up commit kept `selector` core-only; this commit forwards it (both adapter configs, threaded into each per-member `KirokuAdapterConfig` for groups, the same predicate on every member), with an adapter delivery-path test. No `shibuya-core` changes; `Convert.hs`/`AckHandle` untouched (filtering stays worker-side).
  Date: 2026-05-30.


- Decision: Add child plan 5 — OpenTelemetry tracing of subscription state — and correct the Vision & Scope claim that `kiroku-otel` "already adapts `KirokuEvent`."
  Rationale: The user noted that subscription state was never reflected in OTel traces. Research found the deferral note rested on a false premise — `kiroku-otel` only does W3C trace-context propagation, with no `KirokuEvent`→OTel bridge. Tracing is genuinely net-new and is a distinct functional concern (observability), independently verifiable via an in-memory span exporter, so it warrants its own child plan rather than amending a Complete one. It is additive: no `kiroku-store` change, no `hs-opentelemetry` dependency added to the core, consumed through the existing `eventHandler` callback.
  Date: 2026-05-30.

- Decision: EP-5 models subscription state as short per-episode spans (`catchup`/`paused`/`reconnecting`/`retrying`, opened on a state-entry `KirokuEvent` and ended on the matching exit event) plus short per-batch `fetch` spans during `Live` — never one worker-lifetime span. Traces + attributes only; OTel metrics stay deferred.
  Rationale: Verified in `hs-opentelemetry` that a span is exported only when it ends (`endSpan` → `tracerProviderOnEnd` → `spanProcessorOnEnd`; no in-flight/snapshot export). A lifetime span would be invisible on a live worker and lost on crash. Per-episode/per-batch spans end promptly, so completed episodes show their real duration and live work exports continuously. The accepted limitation (an unresolved in-progress episode is not visible until it ends) is covered by the existing `currentState` accessor / `KirokuEvent` logs, and ultimately by a deferred state-gauge metric. The user's request was specifically about traces and attributes; metric emission remains deferred per MasterPlan 5. See Surprises & Discoveries for the source evidence.
  Date: 2026-05-30.

- Decision: EP-5 takes the work end-to-end by populating Shibuya's `Envelope.attributes` (kiroku subscription name, consumer-group member, event type, global position) in `shibuya-kiroku-adapter`, not only native-subscription spans.
  Rationale: User direction (end-to-end). Shibuya's runner already merges `envelope.attributes` into its per-message span (`shibuya-core/src/Shibuya/Runner/Supervised.hs:397-398`), so filling the currently-empty field makes the kiroku identity visible on the Shibuya side of a trace with no `shibuya-core` change — matching this MasterPlan's "end-to-end Shibuya integration" title. The subscription name/member are threaded from the adapter configs into the conversion; event type and position come from the `RecordedEvent`.
  Date: 2026-05-30.


## Outcomes & Retrospective

All five child plans are Complete (EP-1–EP-4 on 2026-05-29; EP-5 on 2026-05-30). The initiative is fully delivered: the subscription worker is an explicit FSM with recoverable pause/reconnect; handlers (native, Streamly-bridged, Shibuya) can retry or dead-letter per event; a kiroku consumer group is presentable as one Shibuya `PartitionedInOrder` subscription; subscriptions can declare an event-type filter (and an opaque `selector`) with the no-stall invariant; and the FSM lifecycle is reflected in OpenTelemetry traces that ride end-to-end through the Shibuya adapter.

- **Subscription-worker FSM (EP-1).** `Kiroku.Store.Subscription.Fsm` defines an explicit `SubscriptionState` (`CatchingUp`, `Live`, `Paused`, `Reconnecting`, `Retrying`, `Stopped`) driven by a single exhaustive `step :: SubscriptionState -> Input -> (SubscriptionState, [Effect])` (compiled `-Werror=incomplete-patterns`), with the current state observable via the `currentState` handle accessor. Two recoverable states were added beyond a formalization of the old phases: `Paused` (recoverable backpressure; default `OverflowPolicy` is now `PauseAndResume`, lossless re-catch-up on drain) and `Reconnecting` (a live fetching worker that loses its pool re-enters catch-up from its checkpoint instead of dying; applies to Category / consumer-group workers — AllStreams live is publisher-fed).
- **Per-event retry / dead-letter (EP-2/EP-40).** `SubscriptionResult` gained `Retry !RetryDelay` and `DeadLetter !DeadLetterReason`; `RetryPolicy`/`defaultRetryPolicy` (5 attempts) bound redelivery. Mechanics live in the one delivery primitive `processEvents` with a surfaced `Retrying` state; dead-letter is an atomic insert-and-checkpoint into the new `kiroku.dead_letters` table (per-`consumer_group_member`). The Streamly bridge became ack-coupled (`subscriptionAckStream`/`AckItem`), and the Shibuya adapter maps `AckOk`/`AckRetry`/`AckDeadLetter`/`AckHalt` onto these via `AckHandle.finalize` (`AckHalt` cancels without advancing).
- **Consumer-group ↔ Shibuya policy (EP-3/EP-42).** `consumerGroupPolicy` reuses Shibuya's `validatePolicy StrictInOrder` (`Serial → (PartitionedInOrder, Serial)`; `Ahead`/`Async → InvalidPolicyCombo`). `kirokuConsumerGroupProcessors` presents a whole group as N policy-pinned Shibuya processors (`ProcessorId "<name>-member-<m>"`), one per member — Shibuya does not route by partition key, so member identity rides the `ProcessorId`, not a bridge item. No `shibuya-core` changes.
- **Event-type filtering (EP-4/EP-43).** `EventTypeFilter (AllEventTypes | OnlyEventTypes !(Set EventType))` + `eventTypeMatches`, an additive `eventTypeFilter` field on `SubscriptionConfigM` (default `AllEventTypes`), applied in `processEvents` before the handler so filtered events skip delivery while the batch-tail checkpoint advances past them (no-stall). Forwarded unchanged through the Streamly bridge and into the adapter / consumer-group configs. A later (2026-05-30) API review kept the closed sum and added a *separate* optional `selector :: Maybe (RecordedEvent -> Bool)` escape hatch for predicate filtering the type set cannot express (composed as a logical AND via `shouldDeliver`, same no-stall guarantee) rather than replacing the sum with a bare function — see the Decision Log for why (SQL pushdown, `Eq`/`Show`, introspectability).
- **OpenTelemetry tracing of subscription state (EP-5/EP-44).** A new `Kiroku.Otel.Subscription` module turns the `KirokuEvent` lifecycle stream into spans via `subscriptionTraceHandler :: Tracer -> IO (KirokuEvent -> IO ())`, installed as the existing `eventHandler` callback — no `kiroku-store` change and no `hs-opentelemetry` dependency added to the core. The span model is short per-episode spans (`catchup`/`paused`/`reconnecting`/`retrying`) plus per-batch `fetch` spans, never a worker-lifetime span, because the OTel SDK only exports a span on `endSpan`; spans are keyed by `(subscription name, member)` in a thread-safe cell and carry a `kiroku.*` attribute set. End to end, `shibuya-kiroku-adapter` fills Shibuya's previously-empty `Envelope.attributes` with the kiroku identity (subscription name, member, event type, global position) so one trace is followable from a kiroku subscription into Shibuya's per-message spans, with matching keys on both sides. OTel *metrics* remain deferred (MasterPlan 5).
- **Test results.** `kiroku-store-test` **0 failures** (FSM/pause/resume/reconnect, retry/dead-letter, consumer groups, and the new `Test.EventTypeFilter` incl. no-stall and filter-before-dead-letter). `shibuya-kiroku-adapter-test` **20 examples, 0 failures** (ack mapping, consumer-group partitioning, event-type-filter and selector delivery, and the new EP-5 envelope-attribute assertions). `kiroku-otel-test` **13 examples, 0 failures** (subscription-state tracing through an in-memory span exporter). `kiroku-store-migrations` dead-letter migration applied by the shared harness. `cabal build all` clean.


## Revision Notes

- 2026-05-30 — EP-5 (OTel tracing) implemented and marked Complete; all five child plans now Complete and the initiative is fully delivered. M1 added `Kiroku.Otel.Subscription.subscriptionTraceHandler` (`kiroku-otel-test` 13 examples, 0 failures); M2 populated Shibuya `Envelope.attributes` in the adapter (`shibuya-kiroku-adapter-test` 20 examples, 0 failures); M3 added the `docs/user/opentelemetry.md` "Tracing Subscription State" section + cross-links, both CHANGELOGs, and confirmed the false "`kiroku-otel` already adapts `KirokuEvent`" claim is corrected. Updated Exec-Plan Registry (row 5 → Complete), Progress (EP-5 M1–M3), Surprises & Discoveries (the api-0.3.0.0-vs-0.4, left-biased-`addAttributes`, same-tag-pin, and DB-test-deferral findings), and Outcomes & Retrospective (initiative fully delivered; EP-5 outcome bullet; test totals). Additive: no `kiroku-store`, `hs-opentelemetry`-on-core, or `shibuya-core` change.
- 2026-05-30 — Added child plan 5 (`docs/plans/44-...`), OpenTelemetry tracing of subscription-worker state and span attributes end to end through the Shibuya adapter, after the user noted subscription state was never reflected in OTel traces. Research found the original Vision & Scope OTel deferral note was inaccurate — it claimed `kiroku-otel` "already adapts `KirokuEvent`," but `kiroku-otel` only contains W3C trace-context propagation, with no `KirokuEvent`→OTel bridge — and verified against `hs-opentelemetry` source that a span exports only when it ends (forcing a short-span model rather than a worker-lifetime span). Updated Vision & Scope (corrected the deferral note; metrics still deferred, tracing now in scope; new outcome bullet), Decomposition Strategy (four → five work streams; EP-5 paragraph), Exec-Plan Registry (row 5, Not Started, soft-deps EP-1–EP-4), Dependency Graph (EP-5 paragraph; order 1 → (2,3,4) → 5), Integration Points (the `KirokuEvent` constructor set + `eventHandler` consumer; Shibuya `Envelope.attributes`), Progress (EP-5 M1–M3), Surprises & Discoveries (the two findings with evidence), Decision Log (three EP-5 decisions), and Outcomes & Retrospective (initiative reopened for EP-5). EP-5 is additive: no `kiroku-store` change and no `hs-opentelemetry` dependency added to the core; no `shibuya-core` change.
- 2026-05-29 — Initial creation: three child plans (EP-1 FSM, EP-2 retry/dead-letter adopting existing EP-40, EP-3 partitioning).
- 2026-05-30 — API review of EP-4's `EventTypeFilter` (before release): evaluated replacing the closed sum with a bare `RecordedEvent -> Bool`. Resolution — keep the introspectable/`Eq`/`Show`/pushdown-able sum and add a separate optional `selector :: Maybe (RecordedEvent -> Bool)` escape hatch (default `Nothing`), composed AND via the new `shouldDeliver`. Added `selector` to `SubscriptionConfigM`, applied it in `processEvents` alongside the type filter, updated all full-literal config sites, and added two selector regression tests (`kiroku-store-test` now 178 examples, 0 failures; adapter 17, 0). Updated Outcomes, Decision Log, and `kiroku-store/CHANGELOG.md`.
- 2026-05-29 — EP-4 (event-type filtering) implemented and marked Complete; all four child plans now Complete. Filled in Outcomes & Retrospective. `kiroku-store-test` 0 failures (new `Test.EventTypeFilter`); `shibuya-kiroku-adapter-test` 17 examples, 0 failures (new single-adapter and consumer-group event-type-filter delivery tests). All EP-4 milestones (M1–M3) done and tested across both packages.
- 2026-05-29 — Added child plan 4 (`docs/plans/43-...`), per-subscription event-type filtering, after the user identified type filtering as another missing capability. Research confirmed kiroku has no subscription-level filtering today and that EventStore's in-memory `selector` advances the checkpoint past filtered events. Updated Vision & Scope (new outcome bullet; mapper excluded), Decomposition Strategy (three → four work streams), Exec-Plan Registry (row 4), Dependency Graph (EP-4 soft-deps EP-1; filter-before-deliver vs EP-2), Integration Points (shared `SubscriptionConfigM`; filter-before-delivery ordering; adapter config forwarding), Progress (EP-4 milestones), and the Decision Log. Reason: keep all subscription-runtime work coordinated under one MasterPlan so the shared config record, FSM delivery transition, bridge, and adapter changes are reconciled rather than colliding.
