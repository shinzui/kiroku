# Append Performance Experiment Log

This file records every append-performance experiment Kiroku has run. It is
**append-only by convention**: new experiments are added as new rows at the
bottom of the table, prior rows are not edited even when later evidence changes
how we interpret them. To revise a prior finding, append a new row whose
**Lesson** column points at the earlier row's date and explains the
reinterpretation. The convention is enforced by code review, not by tooling.
The discipline for proposing a new experiment lives in
[`docs/PERF-METHODOLOGY.md`](PERF-METHODOLOGY.md).

Each row records:

- **Date** — when the experiment was run (ISO `YYYY-MM-DD`).
- **Plan** — full repository-relative path to the ExecPlan that introduced the
  experiment.
- **Hypothesis** — one sentence stating what was expected to improve and why.
- **Variant** — what was actually tested (SQL shape, dispatch path, schema
  change, benchmark-only probe, informal trial).
- **Bench Numbers** — the bench cells and values that supported the
  keep / revert / not-implement decision. Quote figures verbatim from the
  source plan; mark `(qualitative, no CSV)` when no CSV exists.
- **Outcome** — one of `kept`, `reverted`, `not-implemented`.
- **Lesson** — a one-line takeaway.

| Date | Plan | Hypothesis | Variant | Bench Numbers | Outcome | Lesson |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-05-18 | [`docs/plans/21-evaluate-append-hot-path-performance-experiments.md`](plans/21-evaluate-append-hot-path-performance-experiments.md) | A narrow `UPDATE`-first path for existing hot streams, falling through to the existing `INSERT … ON CONFLICT DO UPDATE` only on miss, should remove unnecessary upsert cost on the hot append. | New `AnyVersion` SQL path tries `UPDATE` first; original upsert kept as fallback. Dispatch in `kiroku-store/src/Kiroku/Store/Effect.hs`. | Focused `All.concurrent.32 writers x 10 appends` 1.069s vs 1.055s baseline (~flat); focused `All.reliability-audit.hot invoice-payment 10 AnyVersion appends` 2.07–2.26 ms vs 2.99 ms baseline; full-suite `32 writers x 10 appends` 808 ms (+25% vs `baseline.csv`); full-suite `hot invoice-payment` 1.78 ms (+12% vs baseline); 129/129 `kiroku-store-test` examples pass. Transcripts under `docs/bench/append-hot-path/2026-05-18-anyversion-split-*.txt`. | reverted | Helps repeated appends to an existing hot stream but does not solve fresh-stream `$all` contention; full-suite gate still failed and the added append SQL path was not worth the maintenance cost. |
| 2026-05-18 | [`docs/plans/21-evaluate-append-hot-path-performance-experiments.md`](plans/21-evaluate-append-hot-path-performance-experiments.md) | Passing the event count as a bind parameter instead of recomputing `(SELECT count(*) FROM new_events)` six times per CTE should reduce planner work. | Added `eventCount :: Int64` to `AppendParams`/`appendParamsEncoder`; replaced every `(SELECT count(*) FROM new_events)` in the four append CTEs with the new parameter. | Focused `All.concurrent.32 writers x 10 appends` 1.262s vs 1.055s pre-experiment baseline (worse, not better); 129/129 `kiroku-store-test` examples pass. Transcripts under `docs/bench/append-hot-path/2026-05-18-event-count-*.txt`. | reverted | PostgreSQL evidently collapses the repeated count subqueries against the one-row `new_events` CTE; replacing them with a parameter measured slower, not faster. |
| 2026-05-18 | [`docs/plans/21-evaluate-append-hot-path-performance-experiments.md`](plans/21-evaluate-append-hot-path-performance-experiments.md) | A scalar `VALUES` shape for one-event appends should avoid the array `unnest` overhead and beat the array path on the single-event hot path. | New one-event append SQL statement using scalar `VALUES` instead of array `unnest`; one-event appends dispatched to it. | Focused `All.concurrent.32 writers x 10 appends` 1.140s vs 1.055s baseline (worse); `All.append.single-event.AnyVersion (new stream)` 200 µs vs ~205 µs baseline (within noise); 129/129 `kiroku-store-test` examples pass. Transcripts under `docs/bench/append-hot-path/2026-05-18-one-event-values-*.txt`. | reverted | Added code complexity without improving the primary target; array vs scalar binding inside Kiroku's CTE shape does not move the needle. |
| 2026-05-18 | [`docs/plans/22-optimize-singleton-append-sql-path.md`](plans/22-optimize-singleton-append-sql-path.md) | A purpose-built scalar singleton CTE (no arrays, no `count(*)`) for one-event appends should beat the production array/unnest CTE by ≥ 10 % on `single-event.AnyVersion` because batch machinery is unnecessary for a single event. | `SingletonAppendParams`, four singleton statements (one per `ExpectedVersion`), dispatched through `appendToStream`, `appendToStreamTx`, `runTransactionAppending`. Reverted in `kiroku-store/src/Kiroku/Store/Append.hs`. | Focused-control deltas: `All.append.single-event.AnyVersion (new stream)` 166038525 ps → 189620605 ps (+14.20%); `single-event.NoStream (new stream)` 169827420 ps → 173689501 ps (+2.27%); `batch-10.NoStream` 399379296 ps → 463682617 ps (+16.10%); `batch-100.NoStream` 2574535156 ps → 2589526562 ps (+0.58%); `sequential.10 appends to same stream` 1730798828 ps → 1989978125 ps (+14.97%); `concurrent.32 writers x 10 appends` 784906775000 ps → 955480800000 ps (+21.73%); `reliability-audit.hot invoice-payment 10 AnyVersion appends` 1691260156 ps → 1636809375 ps (-3.22%). Raw-shape control (`All.raw-append-shape.AnyVersion`): scalar singleton (new stream) 261766113 ps vs production arrays/unnest 256862695 ps; scalar singleton (hot stream) 280996533 ps vs production arrays/unnest 240184185 ps. 129/129 `kiroku-store-test` examples pass. | reverted | Scalar-vs-array binding inside Kiroku's CTE shape is a wash; the raw-shape evidence shows production array/unnest is at parity or better than the singleton, so the CTE shape is not the next optimization candidate. The benchmark-only `raw-append-shape.AnyVersion.scalar singleton` cells remain in `kiroku-store/bench/Main.hs` as durable evidence. |
| 2026-05-18 | [`docs/plans/22-optimize-singleton-append-sql-path.md`](plans/22-optimize-singleton-append-sql-path.md) | The `stream_events_notify` AFTER INSERT OR UPDATE trigger on `streams` (which calls `pg_notify` once per affected row) is on the hot append path; disabling it should shrink per-append wall time. | Informal trial — disabled the `stream_events_notify` trigger on `streams`. Not part of plan 22's stated milestones; trial documented in plan 22's Surprises & Discoveries. | (qualitative, no CSV — see plan 22 Surprises & Discoveries) | not-implemented | Quoting plan 22: "Disabling `stream_events_notify` helped more than removing the stored generated `streams.category` column. Notification remains a separate optimization candidate, but it does not explain the gap between raw scalar Kiroku SQL and raw production-shape Kiroku SQL." Worth re-running under the EP-1/EP-2 profiling harnesses to convert this qualitative finding into a numeric one. |
| 2026-05-18 | [`docs/plans/22-optimize-singleton-append-sql-path.md`](plans/22-optimize-singleton-append-sql-path.md) | The `streams.category` column (`GENERATED ALWAYS AS (split_part(stream_name, '-', 1)) STORED`) is recomputed on every UPDATE that touches `streams`; removing it should reduce per-append cost. | Informal trial — dropped the `streams.category` generated column from the schema. Not part of plan 22's stated milestones; trial documented in plan 22's Surprises & Discoveries. | (qualitative, no CSV — see plan 22 Surprises & Discoveries) | not-implemented | Quoting plan 22: "Disabling `stream_events_notify` helped more than removing the stored generated `streams.category` column." Removing `category` alone did not close the raw-scalar-vs-raw-production gap; re-evaluate with profile evidence under EP-1/EP-2 before re-proposing. |
| 2026-05-18 | [`docs/plans/23-restructure-append-into-a-two-round-trip-path.md`](plans/23-restructure-append-into-a-two-round-trip-path.md) | Splitting append into two PostgreSQL round-trips — a small `SELECT stream_id, stream_version, deleted_at` followed by a simpler append CTE keyed on integer `stream_id` — should close most of the gap with upstream Elixir EventStore by removing in-SQL version/soft-delete checks and the `INSERT … ON CONFLICT DO UPDATE` upsert. Acceptance gate: hot-stream variant ≥ 30 % faster than production arrays/unnest. | Benchmark-only proof in `kiroku-store/bench/Main.hs`: two `Session.statement` calls on a pooled connection without `BEGIN/COMMIT` (implicit transactions, mirroring upstream eventstore < 1000-event path). New `raw-append-shape/AnyVersion/two-roundtrip (new stream)` and `…/(hot stream)` cells. No source under `kiroku-store/src/` touched. | `All.raw-append-shape.AnyVersion.production arrays/unnest (new stream)` 153 µs ← baseline; `…two-roundtrip (new stream)` 159 µs (+3.9 %). `…production arrays/unnest (hot stream)` 152 µs ← baseline; `…two-roundtrip (hot stream)` 166 µs (+9.2 %). Command: `cabal bench kiroku-store:kiroku-store-bench --benchmark-options="-p raw-append-shape --csv /tmp/kiroku-raw-append-shape-23.csv"`. | not-implemented | The kill-switch gate (≥ 30 % faster on hot stream) failed: result is 9 % slower, not 30 % faster. Round-trip topology is not the lever on Hasql's stack — per-round-trip cost (~70–80 µs of protocol + encode/decode) outweighs any in-SQL simplification at one extra round-trip. The bench cells remain in `kiroku-store/bench/Main.hs` as durable evidence so future plans reaching for round-trip-topology changes can re-run them. |
| 2026-05-18 | [`docs/plans/23-restructure-append-into-a-two-round-trip-path.md`](plans/23-restructure-append-into-a-two-round-trip-path.md) | Same hypothesis as the previous row, but with the resolve+append pair wrapped in an explicit `BEGIN`/`COMMIT` (`TxSessions.transaction TxSessions.ReadCommitted TxSessions.Write`) to match the optimistic-concurrency contract the production code would require. | Benchmark-only proof in `kiroku-store/bench/Main.hs`: `raw-append-shape/AnyVersion/two-roundtrip + BEGIN/COMMIT (new stream)` and `…/(hot stream)` cells. | `…production arrays/unnest (new stream)` 153 µs ← baseline; `…two-roundtrip + BEGIN/COMMIT (new stream)` 193 µs (+26.1 %). `…production arrays/unnest (hot stream)` 152 µs ← baseline; `…two-roundtrip + BEGIN/COMMIT (hot stream)` 194 µs (+27.6 %). | not-implemented | Explicit `BEGIN`/`COMMIT` adds two more round-trips on top of the resolve+append pair, costing ~28 µs (~one round-trip's worth) on both hot and new streams. Confirms the per-round-trip overhead read in the previous row: round-trip count, not SQL shape, dominates on this stack. |
| 2026-05-18 | [`docs/plans/24-localize-the-hasql-round-trip-overhead.md`](plans/24-localize-the-hasql-round-trip-overhead.md) | If `Hasql.Pool` itself is a fat target, pool acquire/release would account for a meaningful fraction of the ~152 µs single-event append wall time and would be worth investigating before any further SQL or schema change. | Transient `hasql-overhead` `tasty-bench` group in `kiroku-store/bench/Main.hs`; cell `empty Pool.use (pure ())`. Group deleted after the question was answered. | `empty Pool.use (pure ())` 605 ns ± 53 ns (median of three full runs against the same ephemeral PostgreSQL the rest of the harness uses). | not-implemented | `Hasql.Pool` is sub-microsecond — three orders of magnitude smaller than the append. The pool layer is not the bottleneck. |
| 2026-05-18 | [`docs/plans/24-localize-the-hasql-round-trip-overhead.md`](plans/24-localize-the-hasql-round-trip-overhead.md) | If Hasql's wire-protocol round-trip is fat, a bare `SELECT 1` would account for a meaningful share of append wall time and would justify looking at Hasql encoder allocations or prepared-statement caching before further append SQL work. | Transient `hasql-overhead` cell: `SELECT 1` via `Session.statement` on a hot pooled connection. Group deleted after measurement. | `SELECT 1` ~13 µs (median of three runs). That is the floor for any Hasql round-trip on this stack. | not-implemented | A bare round-trip is ~13 µs, far smaller than the ~152 µs production single-event append. Hasql is not the bottleneck; further optimization must target PostgreSQL-side or Kiroku's CTE, not the SDK layer. |
| 2026-05-18 | [`docs/plans/24-localize-the-hasql-round-trip-overhead.md`](plans/24-localize-the-hasql-round-trip-overhead.md) | The marginal cost of a second round-trip on a hot pooled connection should quantify exactly what plan 23 paid for going from one to two round-trips. | Transient `hasql-overhead` cell: two `SELECT 1` statements in one `Session`. Group deleted after measurement. | Two `SELECT 1` in one session ~27 µs ⇒ marginal second round-trip ~14 µs for trivial statements (~22 µs for non-trivial encoders per plan 24's Outcomes). | not-implemented | Directly explains plan 23's negative result: adding a second round-trip costs ~14–22 µs of pure overhead that a simpler second statement cannot recover. Reinforces "keep append at one PostgreSQL round-trip." |
| 2026-06-11 | [`docs/plans/63-decide-the-marten-style-global-position-migration-with-durable-fsync-benchmarks-and-option-preserving-contract-changes.md`](plans/63-decide-the-marten-style-global-position-migration-with-durable-fsync-benchmarks-and-option-preserving-contract-changes.md) | Strategy E's cross-stream append ceiling is `batchSize / durable_commit_latency` because the `$all` row lock is held across the synchronous WAL flush; honest durable fsync (macOS `wal_sync_method=fsync_writethrough`, which issues `F_FULLFSYNC`) should collapse unbatched throughput by **≥ 3×** vs the non-durable default. Predicted ordering `throughput(B) ≪ throughput(A) ≤ throughput(C)`. | M3 Mac falsification sweep: `append-only`, writers=32, batch=1, payload=256, 60 s steady-state window after 15 s warmup, fresh `kiroku` schema migrated per run; three PostgreSQL durability configs differing only in `wal_sync_method`/`synchronous_commit`. nix-built `kiroku-bench` (kiroku-bench @90e0111) against local PostgreSQL 18; kiroku @786282e. Throughput = Δ`bench_workload_ops_total{op="append"}` / 60 s. Single run per config (the effect is large enough that trial variance cannot change the verdict). | Run A (default `open_datasync`, `synchronous_commit=on`): **3,702 ev/s**. Run B (`fsync_writethrough`, honest `F_FULLFSYNC`): **172.8 ev/s**. Run C (`synchronous_commit=off`): **5,086 ev/s**. All three: 0 errors. **A/B = 21.4×**; ordering `B (173) ≪ A (3702) ≤ C (5086)` holds. | confirmed | Causal model **confirmed emphatically**: honest durable fsync collapses unbatched cross-stream throughput **21.4×** (gate required ≥ 3×), to ~173 commits/s ≈ **5.8 ms per fully-serialized durable commit** — the `$all` lock held across the WAL flush defeats PostgreSQL group commit. The macOS "~50K events/s" in `docs/DESIGN.md` is a non-durable-fsync artifact (`open_datasync` does not flush the drive cache). Gate passed → proceed to M4 (sequence-based arm + GCP matrix). |
| 2026-06-11 | [`docs/plans/63-decide-the-marten-style-global-position-migration-with-durable-fsync-benchmarks-and-option-preserving-contract-changes.md`](plans/63-decide-the-marten-style-global-position-migration-with-durable-fsync-benchmarks-and-option-preserving-contract-changes.md) | The sequence-based arm (no `$all` lock) should beat Strategy E by 5–15× at writers=32 under honest fsync, because removing the lock lets concurrent cross-stream commits share WAL flushes via group commit. | M4 local arm + Mac honest-fsync **proxy** (NOT the pre-registered gate, which is GCP). `kiroku-bench-seqproto` (kiroku's `appendAnyVersionSQL` re-qualified to `seqproto.*`, `all_update` CTE removed, `$all` positions from `nextval('seqproto.global_position_seq')`) vs `kiroku-bench` append-only. writers=32, payload=256, 60 s window, `wal_sync_method=fsync_writethrough`, local PostgreSQL 18; kiroku-bench @260e93a, kiroku @786282e. Plus a writer-scaling discriminator and a 5M-row gap-scan. | Honest fsync: seqproto vs E **1.14×** at batch=1 (183.3 vs 160.2 ev/s), **1.04×** at batch=10 (1733 vs 1661), 0 errors. Discriminator: seqproto FLAT across writers (1/8/32 → 177.7/195.5/182.2 ev/s). Default (non-durable) fsync: seqproto ~10.8K vs E ~3.7K (2.9×). Gap-scan on 5M `$all` rows: p50 2.25 ms, p95 2.41 ms (gate p95<25 ms). Invariants: per-stream versions contiguous, `$all` strictly increasing, 0 gaps, 0 errors. | inconclusive (proxy) | The Mac CANNOT measure the gain: under `F_FULLFSYNC` the device serializes durable commits, so group commit never batches concurrent flushes and seqproto throughput is flat in writer count — both arms hit the same ~180 commit/s ceiling, giving a misleading ~1.1×. This is the very reason the verdict is pre-registered on **GCP** (Linux + pd-ssd, where `fdatasync` group commit works). The 2.9× under cheap non-durable fsync shows lock removal *does* help when the flush is not the bottleneck. Gap-scan viability is comfortably met. The GCP matrix remains the gating measurement. |
| 2026-06-11 | [`docs/plans/63-decide-the-marten-style-global-position-migration-with-durable-fsync-benchmarks-and-option-preserving-contract-changes.md`](plans/63-decide-the-marten-style-global-position-migration-with-durable-fsync-benchmarks-and-option-preserving-contract-changes.md) | **The pre-registered gating measurement.** The sequence arm (no `$all` lock) beats Strategy E by 5–15× at writers=32 under honest durable fsync on GCP, because removing the lock lets concurrent cross-stream commits share WAL flushes via group commit. PROCEED requires `G(32,1) ≥ 3.0`; NOT WORTH IT if `G(32,1) < 2.0`. | M4 GCP matrix on `tan-nb-exp`/`us-west1`, PostgreSQL 17.9, pd-ssd, fresh postgres VM+disk per trial: `kiroku-bench` append-only (Strategy E, kiroku `786282e`) vs `kiroku-bench-seqproto` (kiroku-bench `260e93a`); writers=32, payload=256, 180 s steady state, `synchronous_commit=on`, 3 trials/cell, median. load-testing-infra `bf60458`. Verdict resolved against a written target (few-thousand ev/s, latency-sensitive, unbatchable). | **G(32,1) = 1,499 / 687 = 2.18×** (Strategy E b1 681/694/687; seqproto b1 1636/1394/1499). Strategy E b10 = 4,364 ev/s. seqproto FLAT in writers: w8 1,559 → w32 1,499. Both arms ~40–44 % postgres CPU, 100 % cache-hit, disk unsaturated, durable-commit-bound; direct append counts 294,549 (seqproto) vs 122,711 (Strategy E) per identical 180 s window. Gap-scan p95 2.41 ms (< 25 ms gate). | not-implemented | **Predicted 5–15× FALSIFIED → measured 2.18× (judgment zone).** Removing the `$all` lock buys a one-time 2.18× (the lock's own overhead), then hits a second wall: the durable-commit path itself. seqproto is *flat in writer count*, so PostgreSQL group commit does **not** amortize concurrent durable commits on pd-ssd — the same serialization the M3 Mac run found (`F_FULLFSYNC`), reproduced on cloud hardware. Neither single-store design reaches the few-thousand single-event target (Strategy E ~687/s, seqproto ~1,499/s, both flat); **sharding the current architecture reaches it linearly without the rewrite**. An 8–10-plan core rewrite + permanent HWM daemon for a flat 2.18× that still misses the target is **NOT WORTH IT**. Would overturn only if a `commit_delay`/group-commit probe shows the sequence arm scaling past ~1,500/s on one store. Separate finding: current kiroku `786282e` ~30 % below May `c672d58` (~687 vs ~972) on the same PG 17.9 — possible append-path regression, investigate independently. |
